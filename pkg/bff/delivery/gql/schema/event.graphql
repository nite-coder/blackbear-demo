input publishEventInput {
    eventID: Int!
}

enum PublishedStatus {
  DRAFT
  PUBLISHED
}

type Event {
    id: Int!
    title: String!
    description: String!
    publishedStatus: PublishedStatus!
    createdAt: Time!
}